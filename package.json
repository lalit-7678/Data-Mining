{
  "name": "data-mining-system-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix frontend && pip install -r requirements.txt",
    "start": "concurrently \"uvicorn main:app --host 0.0.0.0 --port $PORT\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}